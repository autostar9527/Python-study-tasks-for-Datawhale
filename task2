1.列表
标志
列表的标志符号是[ ]
基本操作
操作有创建，增加元素，插入元素，删除元素。
# 创建列表，通过[]来创建列表
my_list = [] # 创建了一个空列表
2.元组
标志
元组的标志性符号是逗号，不是小括号，但是创建一个空元组，可以直接用小括号。
基本操作
元组是有序列表，一旦初始化就不能修改，没有append（），insert（）方法；
3.string字符串
定义以及基本操作
定义及基本操作：字符串是有0个或者多个字符组成的有限序列。基本操作有修改字符串大小写，字符串拼接，去除空白等。
字符串相关方法：title()用来首字母大写，upper()用来字母全部大写，lower()用来字母全小写，rstrip()删除末尾空白，lstrip()删除开头空白，strip()删除开头和末尾的空白。
4.字符串格式化问题：
%用来实现格式化
常见占位符：
％d：整数，%f：浮点数，%s：字符串，%x：十六进制整数
